import React from 'react';

// container/component
import PasswordContainer from '@app/containers/Auth/Password';

const PasswordScreen = (props: any) => {
  return <PasswordContainer navigation={props.navigation} />;
};

export default PasswordScreen;
