import React from 'react';

// container/component
import VerificationContainer from '@app/containers/Auth/Verification';

const VerificationScreen = (props: any) => {
  return <VerificationContainer navigation={props.navigation} />;
};

export default VerificationScreen;
