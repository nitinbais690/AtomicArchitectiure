import React from 'react';

// container/component
import ReportContainer from '@app/containers/Report';

const ReportScreen = (props: any) => {
  return <ReportContainer navigation={props.navigation} />;
};

export default ReportScreen;
