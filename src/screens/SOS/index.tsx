import React from 'react';

// container/component
import SOSContainer from '@app/containers/SOS';

const SOSScreen = (props: any) => {
  return <SOSContainer navigation={props.navigation} />;
};

export default SOSScreen;
