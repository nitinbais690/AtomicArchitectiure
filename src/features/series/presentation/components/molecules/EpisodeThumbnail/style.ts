import { StyleSheet } from "react-native";
import { heightPercentageToDP as hp } from "react-native-responsive-screen";

export const episodeThumbnailStyles = StyleSheet.create({
  episodeCardImage: {
    width: "28%",
    height: hp("18%"),
  },
});
