import { StyleSheet } from "react-native";
export const seasonsAndEpisodesStyle = StyleSheet.create({
  seasonsAndEpisodesTemplate: {
    flexDirection: "column",
    width: "100%",
    height: "100%",
  },
});
