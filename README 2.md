MceOTT

To Run in Android:
    $ npm install
    $ npm start
    $ react-native run-android

To Run in iOS:
    $ npm install
    $ npm start
    $ react-native run-ios  --simulator "Apple TV" --scheme "Quickplay-tvOS"
